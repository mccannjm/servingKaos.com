{
  "name": "servingkaos.com",
  "private": true,
  "description": "Static site. serve.js previews it, test/verify.js gates it, tools/og-cards.js prints the share cards. Zero runtime dependencies — playwright-core drives the test browser only.",
  "scripts": {
    "serve": "node serve.js",
    "test": "node test/verify.js",
    "cards": "node tools/og-cards.js"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  }
}
